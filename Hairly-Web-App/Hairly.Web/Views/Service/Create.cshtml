@using Hairly.Web.ViewModels.Service

@model ServiceCreateViewModel

@{
    ViewData["Title"] = "Add New Service";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <!-- Header -->
            <div class="mb-4">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item">
                            <a asp-action="Index" class="text-decoration-none">
                                <i class="bi bi-scissors me-1"></i>Services
                            </a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">Add New</li>
                    </ol>
                </nav>
                <h1 class="display-6 fw-bold mb-1">
                    <i class="bi bi-plus-circle text-primary me-2"></i>Add New Service
                </h1>
                <p class="text-muted">Enter service information below</p>
            </div>

            <!-- Form Card -->
            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    <form asp-action="Create" method="post">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>

                        <!-- Service Name -->
                        <div class="mb-4">
                            <label asp-for="Name" class="form-label fw-semibold">
                                Service Name <span class="text-danger">*</span>
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-light">
                                    <i class="bi bi-scissors"></i>
                                </span>
                                <input asp-for="Name" class="form-control form-control-lg"
                                       placeholder="e.g., Men's Haircut" />
                            </div>
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <!-- Description -->
                        <div class="mb-4">
                            <label asp-for="Description" class="form-label fw-semibold">
                                Description <span class="text-muted">(optional)</span>
                            </label>
                            <textarea asp-for="Description" class="form-control" rows="3"
                                      placeholder="Brief description of the service..."></textarea>
                            <span asp-validation-for="Description" class="text-danger small"></span>
                        </div>

                        <!-- Price -->
                        <div class="mb-4">
                            <label asp-for="Price" class="form-label fw-semibold">
                                Price (EUR) <span class="text-danger">*</span>
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-light">
                                    <i class="bi bi-currency-euro"></i>
                                </span>
                                <input asp-for="Price" class="form-control form-control-lg"
                                       type="number" step="0.01" min="5" placeholder="25.00" />
                            </div>
                            <span asp-validation-for="Price" class="text-danger small"></span>
                            <div class="form-text">
                                <i class="bi bi-info-circle me-1"></i>
                                Enter price in Euros
                            </div>
                        </div>

                        <!-- Duration -->
                        <div class="mb-4">
                            <label asp-for="DurationInMinutes" class="form-label fw-semibold">
                                Duration (minutes) <span class="text-danger">*</span>
                            </label>
                            <div class="input-group">
                                <span class="input-group-text bg-light">
                                    <i class="bi bi-clock"></i>
                                </span>
                                <input asp-for="DurationInMinutes" class="form-control form-control-lg"
                                       type="number" min="5" max="480" placeholder="30" />
                            </div>
                            <span asp-validation-for="DurationInMinutes" class="text-danger small"></span>
                            <div class="form-text">
                                <i class="bi bi-info-circle me-1"></i>
                                Typical duration: 15-120 minutes
                            </div>
                        </div>

                        <!-- Action Buttons -->
                        <div class="d-flex gap-2 justify-content-end mt-4 pt-3 border-top">
                            <a asp-action="Index" class="btn btn-lg btn-outline-secondary px-4">
                                <i class="bi bi-x-circle me-2"></i>Cancel
                            </a>
                            <button type="submit" class="btn btn-lg btn-primary px-4">
                                <i class="bi bi-check-circle me-2"></i>Create Service
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
